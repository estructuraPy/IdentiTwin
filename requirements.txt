# Core dependencies
numpy
matplotlib
pandas>=1.3.0

# User interface
tqdm>=4.62.0  # Progress bar visualization

# Hardware interface
adafruit-circuitpython-ads1x15
adafruit-circuitpython-mpu6050>=1.1.0
gpiozero         # For Raspberry Pi GPIO control (LEDs)
adafruit-blinka  # Adafruit hardware compatibility layer (includes board, busio)
mpu6050-raspberrypi # For MPU6050 accelerometer (assuming this specific library)

# Optional dependencies
scipy>=1.7.0  # For signal processing
PyQt5>=5.15.0  # For Qt-based visualization
sphinx_rtd_theme>=1.3.0
psutil           # Optional: For system resource monitoring (CPU, Memory) in PerformanceMonitor
